test contains tests for cnf.

``//test_cnf.py:
``import``
``test_makeload``
``_tryconf``
``

test_makeload checks whether makeload makes a file.

``/test_makeload:
def test_makeload():
    ``.``
``

try yaml.

``
    config = _tryconf("yaml", "a: b")
    assert config["a"] == "b"
``

try ini.

``
    content = """
[a]
b = c
"""
    config = _tryconf("ini", content)
    assert config["a"]["b"] == "c"
``

_createconfig creates a conf file, returns its contents and cleans up.

``/_tryconf:
def _tryconf(fmt, content):
    ``.``
``

remove the output folder if already there.

``
    path = "test/try/conf"
    if os.path.exists("test/try"):
        shutil.rmtree("test/try")
``

`shutil.rmtree()` removes a directory and all its contents. `os.remove()` only
works for files, `pathlib.Path.rmdir()` only works for empty dirs.

create the conf file and check that if exists.

``
    cnf.makeload(path, fmt=fmt, make=content)
    assert os.path.exists(path)
``

load the config.

``
    config = cnf.makeload(path, fmt=fmt, make=content)
``

remove the created files again.

``
    shutil.rmtree("test/try")
``

return for checking.

``
    return config
``

import.

``/import:
import cnf
import os
import shutil
``